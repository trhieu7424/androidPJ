<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NguyenTrongHieu"
        tools:targetApi="31">

        <!-- Activity chính -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Các Activity phụ -->
        <activity
            android:name=".CarListActivity"
            android:label="Danh sách xe hơi" />

        <activity
            android:name=".FoodActivity"
            android:label="Đặt đồ ăn" />

        <activity
            android:name=".ServiceDetailActivity"
            android:label="Chi tiết dịch vụ" />

        <!-- KHAI BÁO MotorbikeListActivity MỘT LẦN DUY NHẤT -->
        <activity
            android:name=".MotorbikeListActivity"
            android:label="Danh sách xe máy" />

    </application>

</manifest>