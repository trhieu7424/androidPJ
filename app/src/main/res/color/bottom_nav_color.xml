<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Màu khi item được chọn (checked) -->
    <item android:state_checked="true" android:color="@color/grab_green" />

    <!-- Màu mặc định cho các item còn lại (không được chọn) -->
    <item android:color="@color/gray" />

</selector>